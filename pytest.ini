[pytest]
pythonpath = .
testpaths = tests
addopts = -q
markers =
    ops_golden: Gate G0 — ops path/money-path contracts; run before full suite
    ops_smoke: critical runtime regression subset (optional marker for later)
